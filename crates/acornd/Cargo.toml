[package]
name = "acornd"
description = "The wire-compatible Cognitum Seed daemon — ties the workspace together."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
acorn-proto = { workspace = true }
