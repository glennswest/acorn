[package]
name = "acorn-sensors"
description = "Reflex rules over the FeatureVector stream that arrives via UDP from the ESP32 fleet."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
acorn-proto = { workspace = true }
parking_lot = { workspace = true }
