//! `acorn-mcp` — MCP proxy exposing the Seed-protocol surface over JSON-RPC 2.0.
//!
//! Phase 4: ~114 tools (seed.memory.*, seed.witness.*, seed.sensor.*,
//!          seed.cognitive.*); default-deny policy. Tool namespaces stay
//!          `seed.*` for wire compatibility with upstream consumers.
//!          Generate tool wrappers from the same handler set as acorn-api.
